import React from 'react'
import {data} from '../restApi.json'
const Menu = () => {
  return (
    <>
      <section className='menu' id='menu'>
        <div className="container">
            <div className="heading_section">
                <h1 className="heading">POPULAR DISHES</h1>
                <p>We celebrate culinary diversity and the joy of discovering new flavors. Explore our curated list of popular dishes that have captured the hearts (and taste buds) of diners like you</p>
            </div>
            <div className="dishes_container">
                {
                    data[0].dishes.map(element => (
                        <div className="card" key={element.id}>
                                <img src={element.image} alt={element.title} />
                                <h2>{element.title}</h2>
                                <h3>{element.price}</h3>
                                <p className='fooddesc'>{element.desc}</p>
                                <button>{element.category}</button>
                        </div>
                    ))
                }   
            </div>
        </div>
      </section>
    </>
  )
}

export default Menu
